- @events.each do |event|
  %div.single_event
    %h2= event.name
    %h3= event.date_and_time.strftime("%A, %B %d, %Y")
    %h3= event.date_and_time.strftime("%I:%M %p").downcase
    %p= event.description